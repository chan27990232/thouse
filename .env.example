VITE_SUPABASE_URL=https://lmrpabhkwfdhdggphktl.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_public_key
# 正式站網域（Vercel Production 建議設定；忘記密碼 redirect 與電郵連結用）
# VITE_PUBLIC_APP_URL=https://thousehk.com
# 租客註冊 API 僅伺服器使用，勿加 VITE_ 前綴、勿提交版控
# 方式 A（Vercel）：在 Vercel → Settings → Environment Variables 新增
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# 方式 B（建議）：部署 Supabase Edge Function，正式站可不設 Vercel 變數
#   npx supabase login
#   npx supabase functions deploy register-tenant --project-ref <專案 ref>
#   npx supabase functions deploy signup-account --project-ref <專案 ref>
# 本機 npm run dev 仍建議在 .env.local 設定 SUPABASE_SERVICE_ROLE_KEY
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# 模擬「今天」以測試租金／付款到期（前端 appToday 邏輯）
# 本機 dev：在 .env.local 設 VITE_MOCK_TODAY 後重啟 npm run dev
# VITE_MOCK_TODAY=2026-07-10
#
# Production 暫時測試（Vercel → Settings → Environment Variables → Production）：
#   VITE_MOCK_TODAY=2026-07-10
#   VITE_ENABLE_MOCK_TODAY=true
# 儲存後 Redeploy；測完請刪除兩個變數並再次 deploy。僅影響前端日期判斷，資料庫 now() 仍是真實時間。

# 本機腳本連線（擇一）：
# A) 建議：Database 頁「Connection string」整段 URI（可選 Pooler，避免 ENOTFOUND）：
#    SUPABASE_DATABASE_URL=postgresql://postgres:密碼@主機:埠/postgres
# B) 或：SUPABASE_DB_PASSWORD=（密碼含空格請用雙引號包住）+ 專案 VITE_SUPABASE_URL
# 勿把密碼或 URI 提交到 Git；用 .env.local

# 忘記密碼：Supabase → Authentication → URL Configuration
#   Site URL: https://thousehk.com/
#   Redirect URLs 加入:
#     https://thousehk.com/
#     http://localhost:3000/   （本機測試）
# 忘記密碼：Supabase → Authentication → SMTP Settings（Office 365）
#   寄件：noreply@thousehk.com / T-House Limited；範本見 supabase/email-templates/reset-password.html
#   Site URL / Redirect URLs：https://thousehk.com/
# Vercel Production：VITE_PUBLIC_APP_URL=https://thousehk.com

# 註冊驗證碼（Vercel /api/signup-verification 或 Supabase signup-verification）
# RESEND_API_KEY=...
# RESEND_FROM_EMAIL=T-House Limited <noreply@thousehk.com>
# Supabase Edge Function Secrets：
#    RESEND_API_KEY、LEASE_REJECTION_FROM_EMAIL（建議 noreply@thousehk.com，需先在 Resend 驗證網域）
#    LEASE_REJECTION_NOTIFY_SECRET、PUBLIC_APP_URL
# 部署：npm run deploy:notify-lease-rejection（需 SUPABASE_ACCESS_TOKEN）
# DB trigger：npm run configure:lease-notify
#    npm run db:lease-rejection-email
#    npm run configure:lease-notify
